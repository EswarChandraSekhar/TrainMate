import { FoundPipePipe } from './found-pipe-pipe';

describe('FoundPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FoundPipePipe();
    expect(pipe).toBeTruthy();
  });
});
